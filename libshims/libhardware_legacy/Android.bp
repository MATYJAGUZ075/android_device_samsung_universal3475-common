cc_library_shared {
    name: "libhardware_legacy",

    // V-6: legacy Exynos3475 gpsd DT_NEEDED libhardware_legacy.so (removed
    // from Android since Q). Full UND audit shows it only needs
    // acquire_wake_lock / release_wake_lock from that library.
    vendor: true,

    srcs: [
        "libhardware_legacy.cpp",
    ],

    stl: "none",
}
